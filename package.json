{
  "name": "ws-expose-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "test": "clear; nodemon;",
    "start": "ts-node server.ts",
    "preinstall": "npm i npm -g; npm i -g typescript; npm i -g ts-node;"
  },
  "author": "Andrew Brantley",
  "license": "ISC",
  "nodemonConfig": {
    "ext": "ts",
    "ignore": [
      "test/*",
      "docs/*"
    ],
    "delay": "100",
    "exec": "ts-node server"
  },
  "dependencies": {
    "@types/node": "^10.5.1",
    "config": "^1.30.0",
    "express": "^4.16.3",
    "ws": "^5.2.1"
  },
  "engines": {
    "node": "6.x"
  }
}
